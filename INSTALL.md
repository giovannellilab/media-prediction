# Installation of dependencies

```bash
mamba create -n media-prediction -y
mamba activate media-prediction

mamba install pandas plotly nbformat seaborn requests biopython tqdm -y
pip install bacdive matplotlib-venn
```
